from sklearn.model_selection import train_test_split

def classify(freq_education_cleaned,freq_income_cleaned,freq_urbanisation_cleaned):
    # I don't know what the target variable should be, so I leave this empty for now
    ...